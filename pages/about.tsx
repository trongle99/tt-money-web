import Layout from '@/components/Layout';
import React from 'react';

const about = () => {
    return (
        <Layout>
            <h1>About page</h1>
        </Layout>
    );
};

export default about;
